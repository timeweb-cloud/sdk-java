

# DbParametersByType

Имена параметров конфигурации, доступных для каждого типа кластера базы данных. Ключ объекта — тип кластера (значение поля `type` при создании кластера), значение — массив имён параметров, которые можно передать в `config_parameters` для кластера этого типа. Наборы параметров различаются между версиями одной СУБД. Значения параметров этот метод не возвращает — рекомендуемые значения можно получить в `GET /api/v1/dbs/default-parameters`.

## Properties

| Name | Type | Description | Notes |
|------------ | ------------- | ------------- | -------------|
|**mysql5** | **List&lt;String&gt;** | Параметры, доступные для кластеров типа &#x60;mysql5&#x60;. |  |
|**mysql** | **List&lt;String&gt;** | Параметры, доступные для кластеров типа &#x60;mysql&#x60;. |  |
|**mysql84** | **List&lt;String&gt;** | Параметры, доступные для кластеров типа &#x60;mysql8_4&#x60;. |  |
|**postgres** | **List&lt;String&gt;** | Параметры, доступные для кластеров типа &#x60;postgres&#x60; (PostgreSQL 13). |  |
|**postgres14** | **List&lt;String&gt;** | Параметры, доступные для кластеров типа &#x60;postgres14&#x60;. |  |
|**postgres15** | **List&lt;String&gt;** | Параметры, доступные для кластеров типа &#x60;postgres15&#x60;. |  |
|**postgres16** | **List&lt;String&gt;** | Параметры, доступные для кластеров типа &#x60;postgres16&#x60;. |  |
|**postgres17** | **List&lt;String&gt;** | Параметры, доступные для кластеров типа &#x60;postgres17&#x60;. |  |
|**postgres18** | **List&lt;String&gt;** | Параметры, доступные для кластеров типа &#x60;postgres18&#x60;. Набор отличается от предыдущих версий PostgreSQL — например, добавлены &#x60;io_method&#x60; и &#x60;io_workers&#x60;. |  |
|**redis** | **List&lt;String&gt;** | Параметры, доступные для кластеров типа &#x60;redis&#x60;. |  |
|**redis7** | **List&lt;String&gt;** | Параметры, доступные для кластеров типа &#x60;redis7&#x60;. |  |
|**redis81** | **List&lt;String&gt;** | Параметры, доступные для кластеров типа &#x60;redis8_1&#x60;. |  |
|**valkey** | **List&lt;String&gt;** | Параметры, доступные для кластеров типа &#x60;valkey&#x60;. |  |
|**valkey7** | **List&lt;String&gt;** | Параметры, доступные для кластеров типа &#x60;valkey7&#x60;. |  |
|**valkey81** | **List&lt;String&gt;** | Параметры, доступные для кластеров типа &#x60;valkey8_1&#x60;. |  |
|**valkey91** | **List&lt;String&gt;** | Параметры, доступные для кластеров типа &#x60;valkey9_1&#x60;. |  |
|**mongodb4** | **List&lt;String&gt;** | Для кластеров типа &#x60;mongodb4&#x60; настраиваемых параметров нет — всегда пустой массив. |  |
|**mongodb** | **List&lt;String&gt;** | Для кластеров типа &#x60;mongodb&#x60; настраиваемых параметров нет — всегда пустой массив. |  |
|**mongodb6** | **List&lt;String&gt;** | Для кластеров типа &#x60;mongodb6&#x60; настраиваемых параметров нет — всегда пустой массив. |  |
|**mongodb7** | **List&lt;String&gt;** | Для кластеров типа &#x60;mongodb7&#x60; настраиваемых параметров нет — всегда пустой массив. |  |
|**mongodb80** | **List&lt;String&gt;** | Для кластеров типа &#x60;mongodb8_0&#x60; настраиваемых параметров нет — всегда пустой массив. |  |
|**opensearch** | **List&lt;String&gt;** | Для кластеров типа &#x60;opensearch&#x60; настраиваемых параметров нет — всегда пустой массив. |  |
|**opensearch219** | **List&lt;String&gt;** | Для кластеров типа &#x60;opensearch2_19&#x60; настраиваемых параметров нет — всегда пустой массив. |  |
|**clickhouse** | **List&lt;String&gt;** | Для кластеров типа &#x60;clickhouse&#x60; настраиваемых параметров нет — всегда пустой массив. |  |
|**clickhouse24** | **List&lt;String&gt;** | Для кластеров типа &#x60;clickhouse24&#x60; настраиваемых параметров нет — всегда пустой массив. |  |
|**clickhouse25** | **List&lt;String&gt;** | Для кластеров типа &#x60;clickhouse25&#x60; настраиваемых параметров нет — всегда пустой массив. |  |
|**kafka** | **List&lt;String&gt;** | Для кластеров типа &#x60;kafka&#x60; настраиваемых параметров нет — всегда пустой массив. |  |
|**rabbitmq** | **List&lt;String&gt;** | Для кластеров типа &#x60;rabbitmq&#x60; настраиваемых параметров нет — всегда пустой массив. |  |
|**rabbitmq40** | **List&lt;String&gt;** | Для кластеров типа &#x60;rabbitmq4_0&#x60; настраиваемых параметров нет — всегда пустой массив. |  |



